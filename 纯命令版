sudo apt update && sudo apt upgrade -y
sudo apt install -y git curl wget gnupg ca-certificates redis-server mariadb-server mariadb-client build-essential pkg-config python3 python3-pip python3-venv python3-dev libffi-dev libssl-dev libjpeg-dev zlib1g-dev liblcms2-dev libblas-dev liblapack-dev wkhtmltopdf
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs
sudo npm install -g yarn
python3 -m pip install --upgrade pip
python3 -m pip install frappe-bench
bench init frappe-bench --frappe-branch version-16
cd frappe-bench
bench get-app erpnext --branch version-16
bench new-site erp.solua.one
bench --site erp.solua.one install-app erpnext
bench --site erp.solua.one migrate
bench --site erp.solua.one enable-scheduler
bench --site erp.solua.one set-maintenance-mode off
bench setup supervisor
bench setup nginx
sudo ln -s $(pwd)/config/supervisor.conf /etc/supervisor/conf.d/frappe-bench.conf
sudo ln -s $(pwd)/config/nginx.conf /etc/nginx/conf.d/frappe-bench.conf
sudo supervisorctl reread
sudo supervisorctl update
sudo systemctl restart nginx
sudo supervisorctl status
